:::note
- The **Forward to SIEM** checkbox is only available if SIEM is installed.
- For C2C sources where the checkbox is already provided, there's no need to add `_siemForward=true` (case-sensitive), since the checkbox alone handles forwarding to SIEM.
:::
