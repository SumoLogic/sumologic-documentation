:::note
- Select the **Forward to SIEM** checkbox only if Cloud SIEM is installed.
- If the checkbox is not available, add `_siemForward=true` manually. Ensure that you use the exact format `_siemForward` as the field name is case-sensitive.
:::